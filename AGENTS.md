# 全局 Agent 行为准则

> 本文件只记录 CC / OpenCode 等工具**默认行为之外**的额外约束。默认已有的内容（并行调用、委托子代理、简洁沟通、优先编辑现有文件等）不再重复。

---

## 联网检索优先

以下场景**必须先用 WebFetch / 搜索工具联网核实**，不得依赖训练数据：

- 工具 / 软件功能咨询（OpenCode、Cursor、VS Code、Claude Code、GitHub Copilot 等是否支持某功能、如何配置）
- 实时性内容：价格、版本号、发布日期、最新 API 变更、Breaking Change、官方公告
- 配置与集成方法：MCP 服务配置、插件安装、环境变量等可能随版本变化的步骤
- 自己对答案不够确定时

**执行方式**：优先官方文档（官网、GitHub README/Wiki、官方博客）；WebFetch 遇重定向立即用新 URL 重试；结论注明来源 URL。

**不需要联网**：语言语法、通用算法、基础概念、当前项目代码内的问题。

---

## GitHub 优先复用

要做一件事或开发一个功能时，**先去 GitHub 搜索是否有现成实现**，再决定自己写还是借鉴/直接使用。

**执行步骤**：

1. 用关键词在 GitHub 搜索相关仓库（`search_repositories` / `search_code`），看 star 数、最近提交、license
2. 评估候选项：能否直接作为依赖引入？或参考其实现思路？是否符合项目协议？
3. 向用户汇报：找到了哪些候选，推荐哪一个、为什么，再开始动手

**何时跳过此规则**：

- 任务本身就是"自己实现某个练习/造轮子"
- 改动极小（如改一行配置、改个文案）
- 项目内部业务逻辑，外部不可能有现成方案

---

## 任务拆分阈值

任务规模超过 **3 个文件改动** 或 **需要读超过 500 行代码** 时，强制走以下流程：

1. **理解**：委托 explore 子模型探索，自己不读原始大文件
2. **规划**：基于 explore 结论拆分子任务（每个 ≤ 3 个文件），定串行/并行顺序
3. **执行**：每个子任务委托 general 子模型，prompt 含目标文件、改动说明、验证方式、需返回的关键结论
4. **整合**：收集结论后判断是否需要修正，不重读已处理文件

---

## 资源保护：禁止全项目 build / test

**禁止**对整个项目执行 build 或 test，避免大型项目占用过多 CPU / 内存：

- 只跑**改动涉及模块**的单元测试（如 `pytest path/to/module`、`go test ./pkg/foo/...`、`npm test -- path/to/file`）
- 只 build **改动涉及的子包 / 单文件**，不跑根级 `build` / `make all` / `npm run build`
- 类型检查同理：用 `tsc --noEmit path/to/file.ts` 而非全项目 `tsc`

**例外**（需先告知用户再执行）：
- 用户明确要求"跑全量测试"或"完整 build"
- 提交前最终验证，且项目本身规模较小（< 50 个源文件）

---

## 上下文保护红线

| 禁止行为 | 替代方案 |
|---|---|
| 读取超过 500 行的文件全文 | 委托 explore 子模型摘要 |
| 在主上下文中逐行分析大型文件 | 委托 general 子模型，只接收结论 |
| 一次性执行超过 5 个文件的改动 | 拆分为多个子任务并行委托 |
| 反复读同一份文件确认细节 | 第一次就让子模型返回完整结论 |

---

## 何时压缩上下文

以下情况发生后**立即**使用 `compress` 工具，不要等上下文快满了再压：

- 一个完整子任务已完成并验证
- 一轮探索结束、结论已提炼
- 一个 Agent 调用已返回结论，原始输出不再需要
